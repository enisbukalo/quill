{
  "kind": "quill.research.technical",
  "version": 1,
  "phase_types": ["producer"],
  "steps": [],
  "allowed_statuses": ["COMPLETE", "INCOMPLETE"],
  "semantic_obligations": ["Record verified versioned technical contracts, evidence, unknowns, and downstream obligations."],
  "payload": {
    "type": "object",
    "required": ["summary", "contracts", "evidence", "unknowns", "obligations"],
    "additional_properties": false,
    "properties": {
      "summary": {"type": "string", "min_length": 1},
      "contracts": {"type": "array", "items": {"type": "string", "min_length": 1}},
      "evidence": {"type": "array", "items": {"type": "string", "min_length": 1}},
      "unknowns": {"type": "array", "items": {"type": "string", "min_length": 1}},
      "obligations": {"type": "array", "items": {"type": "string", "min_length": 1}}
    }
  }
}
